这里为你整理了一份\*\*标准化、最安全（基于 UUID）\*\*的 Linux 磁盘挂载一键留存笔记。你可以直接复制保存，以后新买服务器时，对照着执行即可。

# 📝 Linux 新硬盘挂载标准作业指导（SOP）

本流程适用于 CentOS、Ubuntu、Debian 等主流 Linux 系统，采用 **UUID 挂载法**，能彻底避免服务器重启后盘符漂移导致的系统卡死问题。

### 1. 查看并识别新硬盘

目标：确定新硬盘的设备名称（如 `/dev/vdb` 或 `/dev/sdb`）。

```bash
lsblk
```

> **注意**：在输出中找容量符合、且没有下级分区（没有数字后缀）的那个盘。假设新盘为 `/dev/vdb`。

### 2. 创建磁盘分区

目标：给整块硬盘创建一个主分区。

```bash
fdisk /dev/vdb
```

进入交互界面后，依次输入以下快捷键：

1. 输入 `n` （新建分区）
2. 输入 `p` （选择主分区）
3. 输入 `1` （分区号为 1）
4. 按 `Enter` 回车 （使用默认起始扇区）
5. 再按 `Enter` 回车 （使用默认结束扇区，即占满全盘）
6. 输入 `w` （保存并退出）

此时，再次运行 `lsblk` 就会看到生成了一个新分区：`/dev/vdb1`。

### 3. 格式化分区

目标：刷入 Ext4 文件系统（适合绝大多数 Linux 环境）。

```bash
mkfs.ext4 /dev/vdb1
```

### 4. 创建挂载点（目标文件夹）

目标：创建你想把硬盘挂载到哪个目录下（例如 `/data` 或 `/www`）。

```bash
mkdir -p /data
```

### 5. 获取分区的 UUID（核心安全步骤）

目标：拿到分区的唯一身份证号。

```bash
blkid /dev/vdb1
```

> **输出示例**：`/dev/vdb1: UUID="7f2bf169-953e-489d-9bba-1b0503e714c6" TYPE="ext4" ...` 复制上面属于你的 `UUID="..."` 里面的那一串长字符串。

### 6. 写入开机自动挂载配置（/etc/fstab）

目标：将挂载信息持久化，确保重启不掉盘。

使用编辑器打开配置：

```bash
nano /etc/fstab
```

在文件的**最后一行**，另起一行追加以下内容（**记得将 UUID 替换为你实际获取到的**）：

```text
UUID=7f2bf169-953e-489d-9bba-1b0503e714c6  /data  ext4  defaults  0  0
```

保存并退出编辑器。

### 7. 挂载测试与验证（极重要）

目标：在不重启的情况下测试配置是否正确，防止配置错误导致下次开机失败。

```bash
# 1. 尝试自动挂载 fstab 中的所有设备
mount -a

# 2. 如果上面命令静默返回（没报错），说明配置完美！
# 3. 检查挂载结果和剩余空间
df -h | grep /data
```

**💡 日常维护备忘：**

* 如果后续需要临时解除挂载，使用：`umount /data`。如果提示 busy，可使用 `cd /` 切出目录，或使用 `umount -l /data` 强行解除。
* 如果硬盘大于 2TB，第 2 步的 `fdisk` 建议更换为 `parted` 工具以支持 GPT 分区表。
